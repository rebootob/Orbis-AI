# Configuration

Only safe configuration templates belong in this directory. Real runtime configuration, credentials, tokens, databases, and authentication state must stay local and be ignored by Git.

Use `.env.example` as a field reference; copy values only into an ignored local `.env` file when a future approved implementation phase requires them.
